
  # Revamp Order Management UI

  This is a code bundle for Revamp Order Management UI. The original project is available at https://www.figma.com/design/ifcWaK6tinE2xnodt2VuVU/Revamp-Order-Management-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  